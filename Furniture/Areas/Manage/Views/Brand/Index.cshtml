@model List<Brand>


<div class="card" style="margin:30px">
    <div class="d-flex justify-content-center" style="margin:20px 0px;color:white;">
        <a asp-action="create" class="btn btn-primary">Create </a>
    </div>
    <div class="table-responsive text-nowrap" style="min-height:300px;">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Brand Name</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var brand in Model)
                {
                    <tr>
                        <td> <strong>@brand.Id</strong></td>
                        <td>@brand.Name</td>
                        <td ><img width="70px" height="70px" src="~/uploads/brands/@brand.Image"></td>


                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown"><i class="bx bx-dots-vertical-rounded"></i></button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item delete-action" asp-action="delete" asp-route-id="@brand.Id"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>
<!--/ Hoverable Table rows -->
@section SweatAlert{

    @Html.Partial("SweatAlert")

}

